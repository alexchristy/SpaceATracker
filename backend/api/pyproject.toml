[project]
name = "api"
version = "0.1.0"
description = "SpaceATracker FastAPI API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
core = { workspace = true }
