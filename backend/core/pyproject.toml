[project]
name = "core"
version = "0.1.0"
description = "Shared core models and database functions"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.0.0",
    "pydantic-extra-types>=2.5.0",
    "sqlalchemy>=2.0.46",
]
